Welcome to Ubuntu 20.04 LTS (GNU/Linux 5.4.0-96-generic x86_64)

 * Documentation:  https://help.ubuntu.com
 * Management:     https://landscape.canonical.com
 * Support:        https://ubuntu.com/advantage

  System information as of Sun 16 Mar 2025 06:49:34 PM CST

  System load:  0.18               Processes:             137
  Usage of /:   27.4% of 59.00GB   Users logged in:       0
  Memory usage: 28%                IPv4 address for eth0: 10.0.4.14
  Swap usage:   0%

 * Strictly confined Kubernetes makes edge and IoT secure. Learn how MicroK8s
   just raised the bar for easy, resilient and secure K8s cluster deployment.

   https://ubuntu.com/engage/secure-kubernetes-at-the-edge
New release '22.04.5 LTS' available.
Run 'do-release-upgrade' to upgrade to it.


*** System restart required ***
Last login: Sun Feb 23 12:25:37 2025 from 1.87.242.156
ubuntu@VM-4-14-ubuntu:~$ cat  /etc/nginx/nginx.conf
user root;
worker_processes auto;
pid /run/nginx.pid;
include /etc/nginx/modules-enabled/*.conf;

events {
        worker_connections 768;
        # multi_accept on;
}

http {

        ##
        # Basic Settings
        ##

        sendfile on;
        tcp_nopush on;
        tcp_nodelay on;
        keepalive_timeout 65;
        types_hash_max_size 2048;
        # server_tokens off;

        # server_names_hash_bucket_size 64;
        # server_name_in_redirect off;

        include /etc/nginx/mime.types;
        default_type application/octet-stream;

        ##
        # SSL Settings
        ##

        ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3; # Dropping SSLv3, ref: POODLE
        ssl_prefer_server_ciphers on;

        ##
        # Logging Settings
        ##

        access_log /var/log/nginx/access.log;
        error_log /var/log/nginx/error.log;

        ##
        # Gzip Settings
        ##

        gzip on;

        # gzip_vary on;
        # gzip_proxied any;
        # gzip_comp_level 6;
        # gzip_buffers 16 8k;
        # gzip_http_version 1.1;
        # gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
 
        server {
                listen 80;
                listen [::]:80;
                server_name   www.wflixu.cn;
                  location / {
                        root /home/ubuntu/code/today-pc/dist;
                        index index.html index.htm index.nginx-debian.html;
                        try_files $uri $uri/ $uri/index.html;
                 }
         }
         
        server {
                listen 80;
                listen [::]:80;
                server_name   api.wflixu.cn;
                location / {
                            proxy_pass  http://127.0.0.1:8443; # 转发规则
                            proxy_set_header Host $proxy_host; # 修改转发请求头，让8080端口的应用可以受到真实的请求
                            proxy_set_header X-Real-IP $remote_addr;
                            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                 }
         }


         server {
                listen       443 ssl;

                server_name           api.wflixu.cn;
                ssl_certificate       /home/ubuntu/docs/api.wflixu.cn_bundle.crt;
                ssl_certificate_key   /home/ubuntu/docs/api.wflixu.cn.key;

                ssl_session_cache    shared:SSL:1m;
                ssl_session_timeout  5m;
                client_max_body_size 10m;

                 #    ssl_ciphers  HIGH:!aNULL:!MD5;
                #    ssl_prefer_server_ciphers  on;

                location / {
                        #add_header Access-Control-Allow-Origin *;
                        #add_header Access-Control-Allow-Methods 'GET,POST,DELETE,OPTIONS,PUT,HEAD'; # 改写路径
                         proxy_pass  http://127.0.0.1:8443; # 转发规则
                         proxy_set_header Host $host; # 修改转发请求头，让8080端口的应用可以受到真实的请求
                         proxy_set_header X-Real-IP $remote_addr;
                         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                         proxy_set_header Access-Control-Allow-Origin *;  # 允许任何来源的请求  
                         proxy_set_header Access-Control-Allow-Methods "GET,POST,OPTIONS,DELETE,PUT,HEAD";  # 允许的请求方法  
                 }
         }
         # blog
         server {
            gzip on;
            gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

           listen       443 ssl;
           server_name   blog.wflixu.cn;
           ssl_certificate      /home/ubuntu/docs/blog.wflixu.cn_bundle.crt;
           ssl_certificate_key  /home/ubuntu/docs/blog.wflixu.cn.key;

           ssl_session_cache    shared:SSL:1m;
           ssl_session_timeout  5m;
           client_max_body_size 10m;

           location / {
               root /home/ubuntu/code/nblog/dist;
               index index.html index.htm index.nginx-debian.html;
               try_files $uri $uri/ $uri/index.html =404;

               # non existent pages
               error_page 404 /404.html;

              # a folder without index.html raises 403 in this setup
              error_page 403 /404.html;

              # adjust caching headers
              # files in the assets folder have hashes filenames
              location ~* ^/assets/ {
                  expires 1y;
                  add_header Cache-Control "public, immutable";
              }
          }
       }

        # home 
        server {
                listen       443 ssl;

                server_name   www.wflixu.cn;
                ssl_certificate      /home/ubuntu/docs/wflixu.cn_bundle.crt;
                ssl_certificate_key  /home/ubuntu/docs/wflixu.cn.key;

                ssl_session_cache    shared:SSL:1m;
                ssl_session_timeout  5m;
                client_max_body_size 10m;

                 #    ssl_ciphers  HIGH:!aNULL:!MD5;
                #    ssl_prefer_server_ciphers  on;

                location / {
                        root /home/ubuntu/code/today-pc/dist;
                        index index.html index.htm index.nginx-debian.html;
                        try_files $uri $uri/ $uri/index.html;
                 }
        }
  

        ##
        # Virtual Host Configs
        ##

        include /etc/nginx/conf.d/*.conf;
        include /etc/nginx/sites-enabled/*;
       
        include /home/ubuntu/code/nblog/nginx.conf;
        include /home/ubuntu/code/qdazi/nginx.conf;
}


#mail {
#       # See sample authentication script at:
#       # http://wiki.nginx.org/ImapAuthenticateWithApachePhpScript
# 
#       # auth_http localhost/auth.php;
#       # pop3_capabilities "TOP" "USER";
#       # imap_capabilities "IMAP4rev1" "UIDPLUS";
# 
#       server {
#               listen     localhost:110;
#               protocol   pop3;
#               proxy      on;
#       }
# 
#       server {
#               listen     localhost:143;
#               protocol   imap;
#               proxy      on;
#       }
#}